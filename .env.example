# Copy to .env (gitignored). Client-side values only — EXPO_PUBLIC_* is
# bundled into the app. Anthropic/ElevenLabs keys are server-side only:
# set them with `supabase secrets set` (see supabase/README.md).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_REVENUECAT_KEY=
